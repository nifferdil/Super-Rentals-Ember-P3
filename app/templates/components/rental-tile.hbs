<li>{{#link-to 'rental' rental.id}}{{rental.owner}}'s {{rental.type}} in {{rental.city}} {{/link-to}}
  {{#if isImageShowing}}
  <p>
    <img src={{rental.image}} alt={{rental.type}} {{action 'iamgeHide'}} />
  </p>
  {{else}}
  <<button {{action 'imageShow'}}>Image</button>
  {{/if}}
</li>
